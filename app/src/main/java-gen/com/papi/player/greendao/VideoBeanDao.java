package com.papi.player.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import com.papi.player.javabean.VideoBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "VideoBean".
*/
public class VideoBeanDao extends AbstractDao<VideoBean, String> {

    public static final String TABLENAME = "VideoBean";

    /**
     * Properties of entity VideoBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Cover_url = new Property(0, String.class, "cover_url", false, "COVER_URL");
        public final static Property Enter_id = new Property(1, String.class, "enter_id", true, "ENTER_ID");
        public final static Property CId = new Property(2, String.class, "cId", false, "C_ID");
        public final static Property CCode = new Property(3, String.class, "cCode", false, "C_CODE");
        public final static Property Name = new Property(4, String.class, "name", false, "NAME");
        public final static Property Time = new Property(5, String.class, "time", false, "TIME");
    };


    public VideoBeanDao(DaoConfig config) {
        super(config);
    }
    
    public VideoBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"VideoBean\" (" + //
                "\"COVER_URL\" TEXT," + // 0: cover_url
                "\"ENTER_ID\" TEXT PRIMARY KEY NOT NULL ," + // 1: enter_id
                "\"C_ID\" TEXT," + // 2: cId
                "\"C_CODE\" TEXT," + // 3: cCode
                "\"NAME\" TEXT," + // 4: name
                "\"TIME\" TEXT);"); // 5: time
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_VideoBean_ENTER_ID ON VideoBean" +
                " (\"ENTER_ID\");");
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"VideoBean\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, VideoBean entity) {
        stmt.clearBindings();
 
        String cover_url = entity.getCover_url();
        if (cover_url != null) {
            stmt.bindString(1, cover_url);
        }
 
        String enter_id = entity.getEnter_id();
        if (enter_id != null) {
            stmt.bindString(2, enter_id);
        }
 
        String cId = entity.getCId();
        if (cId != null) {
            stmt.bindString(3, cId);
        }
 
        String cCode = entity.getCCode();
        if (cCode != null) {
            stmt.bindString(4, cCode);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(5, name);
        }
 
        String time = entity.getTime();
        if (time != null) {
            stmt.bindString(6, time);
        }
    }

    /** @inheritdoc */
    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1);
    }    

    /** @inheritdoc */
    @Override
    public VideoBean readEntity(Cursor cursor, int offset) {
        VideoBean entity = new VideoBean( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // cover_url
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // enter_id
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // cId
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // cCode
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // name
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // time
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, VideoBean entity, int offset) {
        entity.setCover_url(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setEnter_id(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setCId(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setCCode(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setName(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setTime(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    /** @inheritdoc */
    @Override
    protected String updateKeyAfterInsert(VideoBean entity, long rowId) {
        return entity.getEnter_id();
    }
    
    /** @inheritdoc */
    @Override
    public String getKey(VideoBean entity) {
        if(entity != null) {
            return entity.getEnter_id();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
